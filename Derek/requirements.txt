# Runtime + extraction. Deliberately minimal: derek/corpus/ and derek/extract/
# are stdlib-only so the snapshot workflow can run without the pipeline deps.
# Detection and interface dependencies are added as those layers land.
