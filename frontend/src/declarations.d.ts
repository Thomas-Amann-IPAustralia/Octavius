// Type shims for packages without bundled declarations

// mammoth ships its own types via @types/mammoth; no shim needed.
// docx ships its own types; no shim needed.
// This file is kept for future additions.
export {}
